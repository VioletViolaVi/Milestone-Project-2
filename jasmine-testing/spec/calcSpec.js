// test to see if false is a boolean
let stopFlip = false;
describe("Check to see if stopFlip is a boolean.", function () {
  it("It should be a boolean.", function () {
    expect(stopFlip).toBe(false);
  });
});
